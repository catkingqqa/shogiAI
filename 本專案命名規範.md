# 本專案命名規範

本文件整理目前專案已採用的命名風格，作為後續新增程式碼時的統一依據。

## 1. 通用原則

- 名稱應清楚表達用途，避免過短或無意義的縮寫。
- 已在專案中固定使用的領域縮寫可以保留，例如：
  - `csa`
  - `sfen`
  - `usi`
  - `ply`
  - `npz`
- 同一概念在不同檔案中應盡量使用相同名稱。
- 布林值應優先使用能表達判斷意義的名稱，例如：
  - `is_check`
  - `has_board_rows`
  - `skip_existing`
  - `orient_to_turn`

## 2. Python 命名規範

### 2.1 檔名

Python 檔案使用 `snake_case`：

```text
csa_preprocess.py
import_csa_to_db.py
csa_browser_api.py
notebook_viewer.py
```

### 2.2 變數

一般變數使用 `snake_case`：

```python
game_index
all_states
move_rows
player_cache
```

### 2.3 函式

函式使用 `snake_case`，名稱以動作開頭：

```python
parse_args()
encode_state()
build_position_row()
serialize_game_summary()
```

常見前綴：

- `parse_`：解析資料
- `build_`：建立資料結構
- `load_`：讀取既有資料
- `ensure_`：確認資料存在，必要時建立
- `serialize_`：轉為可輸出的格式
- `import_`：匯入資料
- `render_`：產生顯示內容

### 2.4 類別

類別使用 `PascalCase`：

```python
SampleMeta
InvalidGame
ImportSummary
MySqlSource
BoardSample
```

### 2.5 常數

常數使用 `UPPER_SNAKE_CASE`：

```python
BOARD_SIZE
MOVE_LABELS
CREATE_TABLES_SQL
HAND_ORDER
```

### 2.6 型別別名

型別別名使用 `PascalCase`：

```python
PlayerCache
```

## 3. JavaScript 命名規範

### 3.1 變數

JavaScript 變數使用 `camelCase`：

```javascript
currentGameId
currentPly
latestState
moveListCount
```

### 3.2 函式

JavaScript 函式使用 `camelCase`，名稱以動作開頭：

```javascript
loadGames()
loadPosition()
renderBoard()
renderMoveList()
toggleMarkedPly()
```

### 3.3 DOM 變數

DOM 元素變數可依目前風格使用語意名稱：

```javascript
gameSelect
boardEl
searchBtn
clearSearchBtn
```

常見尾碼：

- `El`：一般元素
- `Btn`：按鈕
- `Filter`：篩選輸入欄位

## 4. HTML 與 CSS 命名規範

### 4.1 HTML `id`

HTML `id` 使用 `camelCase`：

```html
gameSelect
blackHands
moveListCount
```

### 4.2 CSS `class`

CSS class 使用 `kebab-case`：

```css
game-picker
search-panel
move-list-panel
control-btn
```

### 4.3 狀態 class

狀態 class 使用簡短且可讀的名稱：

```css
active
marked
promoted
last-from
last-to
```

## 5. 文件與資料檔命名

### 5.1 Markdown 文件

專案說明文件可使用大寫或中文名稱：

```text
README.md
BROWSER.md
IMPORT_CSA_DB.md
本專案命名規範.md
```

### 5.2 資料檔

CSA 棋譜檔使用固定流水號格式：

```text
game0001.csa
game0002.csa
game0099.csa
```

若新增輸出檔，建議使用能描述內容的 `snake_case`：

```text
game1_invalid.jsonl
samples.npz
```

## 6. 專案內已採用的固定詞彙

為了保持一致，以下概念建議延續既有用字：

| 概念 | 建議名稱 |
| --- | --- |
| 棋局 | `game` |
| 手數 | `ply` |
| 走法 | `move` |
| 局面 | `position` |
| 棋盤 | `board` |
| 持駒 | `hands` |
| 先手 / 後手 | `black` / `white` |
| 初始資料來源 | `source` |
| 對局結果 | `result` |
| 棋譜摘要 | `summary` |

## 7. 範例

### Python

```python
STATE_PLANES = 43


class GameSummary:
    pass


def build_game_summary(game_id: int) -> dict[str, str]:
    move_count = 0
    has_result = False
    return {
        "game_id": str(game_id),
        "move_count": str(move_count),
        "has_result": str(has_result),
    }
```

### JavaScript

```javascript
const gameSelect = document.querySelector("#gameSelect");
let currentGameId = "";

function loadCurrentGame() {
  return fetch(`/api/games/${currentGameId}`);
}
```

### HTML / CSS

```html
<section id="moveList" class="move-list-panel"></section>
```

```css
.move-list-panel {
  display: grid;
}
```

## 8. 補充說明

- `MySqlSource`、`MySqlConfig` 目前是專案既有寫法，後續建議維持一致。
- 若未來要全面改為 `MySQLSource`、`MySQLConfig`，應一次統一修改，不要兩種寫法混用。
